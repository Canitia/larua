{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}
<div>

      <div class="main-content main-content-head">
        <div class="tag_heading text-center">
          <img src="{{asset "images/error-image.jpg"}}" alt="{{t "Error"}}" title="{{t "Error"}}" class="error-image" />
          <h1 class="text-center tag-title">{{t "Error"}}</h1>
          <pre>{{statusCode}} — {{message}}</pre>
          {{t "This post/page doesn't exist anymore or has been made unavailable."}}
          <div class="card-actions-bottom">
            <a class="post-options-url" href="/">{{t "Return to the homepage"}}</a>
          </div>    
        </div>
      </div>
</div>